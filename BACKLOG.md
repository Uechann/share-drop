# ShareDrop QA·기능 백로그

> 2026-07-15 사용자 테스트(워넬·로지·재키) 피드백 기반. 우선순위순.

| # | 항목 | 제보자 | 우선순위 | 상태 |
|---|------|--------|----------|------|
| 1 | 사진첩 직접 저장 | 워넬·로지·재키 | 최우선 | ✅ 완료 (2026-07-15) |
| 2 | 연속/다중 선택 + 전체 다운로드 | 워넬·로지 | 높음 | ✅ 완료 (2026-07-15) |
| 3 | 닉네임 입장 입력 | 3연속 요청 | 높음 | ✅ 완료 (2026-07-15) |
| 4 | 대규모 스케일 UI | 로지 | 높음 | ✅ 완료 (2026-07-15) · Storage 전환은 후속 과제 |
| 5 | 암호(입장 보안) 설정 | 재키 | 중간 | ✅ 완료 (2026-07-15) |
| 6 | 모바일 인터랙션 버그 (두 번 탭) | 워넬 | 중간 | ✅ 완료 (2026-07-15) |
| 7 | 사진 삭제 기능 + 권한 정책 | 재키 | 중간 | ✅ 완료 (2026-07-15) |
| 8 | 유효기간 옵션 확장 | 반복 요청 | 낮음 | ✅ 완료 (2026-07-15) · TTL 정책은 콘솔 설정 필요 |
| 9 | 폴더→폴더 이동 로직 완성 | 내부 QA | 낮음 | ✅ 완료 (2026-07-15) |

---

## 1. [최우선] 사진첩 직접 저장

**문제**: 모바일에서 다운로드하면 사진첩이 아니라 '파일'로 저장됨. 구글 드라이브를 버린 이유와 동일해져 차별점이 무너짐.

**해결 방안**: 터치 기기에서는 Web Share API(`navigator.share({ files })`)로 공유 시트를 띄움 → iOS/Android 공유 시트의 "이미지 저장"으로 **사진첩에 바로 저장**. 단일 다운로드 버튼과 별도의 "사진첩 저장"(선택 항목 일괄) 버튼 모두 적용. 미지원 브라우저는 기존 파일 다운로드로 폴백.

**한계**: 웹 표준상 브라우저가 사진첩에 무단 쓰기는 불가능하므로 공유 시트 1탭을 거치는 것이 웹앱의 최선. 그마저 없애려면 네이티브 앱(또는 PWA + 네이티브 브릿지)이 필요.

## 2. 연속/다중 선택 + 전체 다운로드

**요구**: Shift 연속선택, Cmd/Ctrl 개별선택, 드래그 선택, 전체 다운로드.

**해결 방안**:
- Shift+클릭: 마지막 선택 항목부터 현재 항목까지 범위 선택
- 개별 토글: 클릭(체크박스 기본 동작) — Cmd/Ctrl 불필요하게 동일 동작
- 드래그(러버밴드) 선택: 갤러리 빈 곳에서 드래그하면 파란 선택 박스, 겹친 카드 선택 (데스크톱)
- "전체 선택" 버튼: 현재 필터 기준 전체 선택/해제 토글 → 전체 다운로드는 전체 선택 후 .zip

## 3. 닉네임 입장 입력

**해결 방안**: 링크/코드로 방에 처음 입장할 때 닉네임 입력 모달 표시(기존 자동 생성 이름이 기본값). 한 번 정하면 저장되어 다시 묻지 않음. 홈 화면의 "내 이름" 필드와 동일한 저장소 사용.

## 4. 대규모 스케일 UI (100명+)

**문제**: 100여 명이 올리면 사용자 목록·폴더 나열·갤러리가 무너짐.

**해결 방안(이번 적용)**:
- 갤러리 점진 렌더링: 30개씩 렌더 + "더 보기" 버튼 (Base64 이미지 수백 장 일괄 렌더 방지)
- 업로더 필터: 업로드 수 기준 내림차순 정렬 + 인원별 개수 표시
- 폴더 칩: 개수 배지 + 가로 스크롤

**남은 과제(실서비스 전 필수)**: Base64→Firebase Storage 전환 + 썸네일 생성, 가상 스크롤, 이미지 목록 쿼리 페이지네이션(Firestore `limit`/`startAfter`). 현재 구조는 방 전체 이미지를 실시간 구독하므로 수백 장 규모에서 트래픽·메모리 병목.

## 5. 암호(입장 보안) 설정

**해결 방안**: 방 생성 시 선택적 암호 설정(SHA-256 해시로 저장, 원문 저장 안 함). 입장 시 암호 모달 → 일치해야 입장(방장은 통과, 세션 내 재입력 불필요). 방장은 설정에서 암호 변경/해제 가능.

**한계**: 클라이언트 검증 + 공개 Firestore 규칙이므로 데모 수준 보안. 실서비스는 Security Rules/서버 검증 필요.

## 6. 모바일 인터랙션 버그 (두 번 탭)

**문제**: 카드 버튼이 호버 시에만 표시되어 모바일에서 첫 탭 = 호버, 둘째 탭 = 클릭.

**해결 방안**: `@media (hover: none)`에서 카드 액션 버튼(크게 보기/다운로드/삭제)을 항상 표시.

## 7. 사진 삭제 기능 + 권한 정책

**정책 결정**: **본인이 올린 파일은 본인이, 방장은 모든 파일을 삭제 가능.** (등록은 방 설정의 업로드 권한을 따름 — 기존과 동일)
근거: 누구나 남의 사진을 지울 수 있으면 대규모 방에서 사고 위험, 방장 단독이면 정리 부담. 절충안.

**해결 방안**: 카드에 삭제 버튼(권한 있을 때만 표시) + 확인 모달. "선택 삭제" 일괄 버튼(권한 없는 항목은 자동 제외 후 안내).

## 8. 유효기간 옵션 확장

**해결 방안**: 3일/7일 옵션 추가 (생성 시 + 방장 설정 변경 모두).

**DB 용량 단서(로지·재키)에 대한 답**:
- 현재 만료는 "입장 차단"일 뿐 데이터가 실제로 지워지지 않음 → Firestore 콘솔에서 `expiresAt` 필드에 **TTL 정책**을 걸면 만료 문서 자동 삭제됨(무료, 권장). 단 하위 컬렉션(images)은 TTL이 안 지워주므로 이미지 문서에도 만료 필드를 복제해 TTL을 걸어야 함 → 이번에 이미지 문서에 `expiresAt` 복제 저장 적용.
- 장기 보관(7일)은 Base64 구조상 용량 부담 큼 → Storage 전환과 함께 재검토.

## 9. 폴더→폴더 이동 로직 완성

**해결 방안**: 선택 후 "폴더로 이동" 드롭다운 정비 — 기존 폴더로 이동, 폴더에서 꺼내기, **"+ 새 폴더 만들어 이동"**(이름 입력 즉시 생성+이동) 지원. 폴더 칩에 파일 개수 표시로 이동 결과 즉시 확인 가능.

---

## 2차 피드백 반영 (2026-07-22)

| 항목 | 내용 | 상태 |
|---|---|---|
| 라이트박스 키보드 조작 | ← → 로 사진 이동, ESC 로 닫기, 이전/다음 버튼 + 위치 표시(n/N) 추가 | ✅ 완료 |
| 드래그 선택 해제 버그 | 빈 공간을 '클릭'하면(드래그 임계값 8px 미만) 선택이 유지돼 풀 방법이 없던 문제 → 빈 공간 클릭 시 전체 선택 해제 | ✅ 완료 |
| 갤럭시 사진첩 저장 불가 | **원인**: 크기 0인 더미 File로 `canShare()`를 미리 검사했는데 일부 안드로이드가 false 반환 → 기능 전체 비활성화. API 존재 여부만 확인하고 실제 파일로 호출 시점 검사하도록 변경 | ✅ 완료 (실기기 확인 필요) |
| 갤럭시 전체 다운로드 불가 | 다중 파일 공유가 거부되면 에러 토스트만 띄우고 끝났던 것 → 단일 공유 재시도 → .zip 다운로드로 단계적 폴백 | ✅ 완료 (실기기 확인 필요) |

⚠️ **미완료 (콘솔 설정 필요)**: TTL 정책이 아직 적용되지 않아 **만료된 방/사진이 실제로 삭제되지 않고 누적 중**입니다. SECURITY.md의 TTL 설정 참고.

---

**결정 사항**: 로그인 기능은 만들지 않음 — 방 코드로 충분하다는 데 다수 공감(워넬·로지·재키).
